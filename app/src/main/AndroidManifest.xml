<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.jaagrt_ui">

    <application
        android:name=".AppExtn"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Login"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Signup"
            android:label="@string/title_activity_signup"></activity>
        <activity
            android:name=".AdditionInfo"
            android:label="@string/title_activity_addition_info"></activity>
        <activity
            android:name=".VerifyPhone"
            android:label="@string/title_activity_verify_phone"></activity>
        <activity
            android:name=".ChoosePic"
            android:label="@string/title_activity_choose_pic"></activity>
    </application>

</manifest>
